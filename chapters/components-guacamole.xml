<?xml version="1.0" encoding="UTF-8"?>
<section id="guacamole" 
    xmlns="http://docbook.org/ns/docbook" version="5.0" xml:lang="en"
    xmlns:xi="http://www.w3.org/2001/XInclude">

    <title>Guacamole</title>

    <para>
        Guacamole is a web application which uses the API provided by the
        Guacamole project (<literal>guacamole-common</literal>,
        <literal>guacamole-common-ext</literal>, and
        <literal>guacamole-common-js</literal>) to implement a universal remote
        desktop gateway and client.
    </para>

    <para>
        Guacamole is written in Java and is released under the <link
            linkend="agpl">GNU Affero General Public License v3.</link>
    </para>

    <section>
        <title>Downloading Guacamole</title>

        <para>
            Pre-compiled packages may be available for your distribution
            from the files section of the main project page. If
            pre-compiled packages are not available for your distribution,
            or you wish to compile Guacamole from source, you can download
            the source from SourceForge, or from the project git
            repositories:
        </para>

        <programlisting>
<command>git <option>clone</option> <filename>git://guacamole.git.sourceforge.net/gitroot/guacamole/guacamole</filename></command>
        </programlisting>

        <para>
            The <filename>master</filename> branch will have the latest stable,
            released code, while the <filename>unstable</filename> branch will
            have the latest development code.
        </para>
    </section>

    <section>
        <title>Compiling and installing Guacamole</title>

        <para>
            Guacamole is built using Maven. Building Guacamole compiles all
            classes and packages them into a deployable
            <filename>.war</filename> file. This <filename>.war</filename>
            file can be installed and deployed under servlet containers like
            Apache Tomcat or Jetty.
        </para>

        <orderedlist>

            <listitem>
                <para>
                    Run <command>mvn <option>package</option></command>
                </para>

                <programlisting>
<prompt>$</prompt> <command>mvn <option>package</option></command>
                </programlisting>

                <para>
                    Maven will download any needed dependencies for building
                    the <filename>.war</filename> file. Once all dependencies
                    have been downloaded, the <filename>.war</filename> file
                    will be created in the <filename>target/</filename>
                    subdirectory of the current directory.
                </para>
            </listitem>

            <listitem>
                <para>
                    Copy the <filename>.war</filename> file as directed in the
                    instructions provided with your servlet container.
                </para>

                <para>
                    Apache Tomcat, Jetty, and other servlet containers have
                    specific and varying locations that
                    <filename>.war</filename> files must be placed for the web
                    application to be deployed.
                </para>

                <para>
                    You will likely need to do this as root.
                </para>
            </listitem>

        </orderedlist>
    </section>
</section>
