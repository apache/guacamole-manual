<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook V5.0//EN"
    "http://docbook.org/xml/5.0/dtd/docbook.dtd">

<refentry id="move-instruction" 
    xmlns="http://docbook.org/ns/docbook" version="5.0" xml:lang="en"
    xmlns:xi="http://www.w3.org/2001/XInclude">

    <indexterm>
        <primary>move</primary>
    </indexterm>

    <refmeta>
        <refentrytitle>move</refentrytitle>
    </refmeta>

    <refnamediv>
        <refname>move</refname>
        <refpurpose>Moves the given layer to the given location within the specified parent layer.</refpurpose>
    </refnamediv>

    <refsynopsisdiv>

        <title>Synopsis</title>

        <para>
            <command>
                move
                <parameter>layer</parameter>
                <parameter>parent</parameter>
                <parameter>x</parameter>
                <parameter>y</parameter>
                <parameter>z</parameter>
            </command>
        </para>

    </refsynopsisdiv>

    <refsection>
        <title>Description</title>
        <para>Moves the given layer to the given location within the specified parent layer. This operation is applicable only to layers, and cannot be applied to buffers (layers with negative indices). Applying this operation to the default layer (layer 0) also has no effect.</para>
    </refsection>

    <refsection>

        <title>Parameters</title>

        <variablelist>
            <varlistentry>
                 <term><parameter>layer</parameter></term>
                 <listitem><para>The layer to move.</para></listitem>
             </varlistentry>
            <varlistentry>
                 <term><parameter>parent</parameter></term>
                 <listitem><para>The layer that should be the parent of the given layer.</para></listitem>
             </varlistentry>
            <varlistentry>
                 <term><parameter>x</parameter></term>
                 <listitem><para>The X coordinate to move the layer to.</para></listitem>
             </varlistentry>
            <varlistentry>
                 <term><parameter>y</parameter></term>
                 <listitem><para>The Y coordinate to move the layer to.</para></listitem>
             </varlistentry>
            <varlistentry>
                 <term><parameter>z</parameter></term>
                 <listitem><para>The relative Z-ordering of this layer. Layers with larger values will appear above layers with smaller values.</para></listitem>
             </varlistentry>
        </variablelist>

    </refsection>

    <refsection>

        <title>Example</title>

        <programlisting>[example]</programlisting>

    </refsection>

</refentry>