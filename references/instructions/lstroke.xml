<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook V5.0//EN"
    "http://docbook.org/xml/5.0/dtd/docbook.dtd">

<refentry id="lstroke" 
    xmlns="http://docbook.org/ns/docbook" version="5.0" xml:lang="en"
    xmlns:xi="http://www.w3.org/2001/XInclude">

    <refmeta>
        <refentrytitle>lstroke</refentrytitle>
    </refmeta>

    <refnamediv>
        <refname>lstroke</refname>
        <refpurpose>Strokes the current path with a tiled pattern of the image data from the specified layer.</refpurpose>
    </refnamediv>

    <refsynopsisdiv>

        <title>Synopsis</title>

        <para>
            <command>
                lstroke
                <parameter>MASK</parameter>
                <parameter>LAYER</parameter>
                <parameter>CAP</parameter>
                <parameter>JOIN</parameter>
                <parameter>THICKNESS</parameter>
                <parameter>SRCLAYER</parameter>
            </command>
        </para>

    </refsynopsisdiv>

    <refsection>
        <title>Description</title>
        <para>Strokes the current path with a tiled pattern of the image data from the specified layer. This instruction completes the current path. Future path instructions will begin a new path.</para>
    </refsection>

    <refsection>

        <title>Example</title>

        <programlisting>[example]</programlisting>

    </refsection>

</refentry>